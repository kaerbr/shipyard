name: 🚢 Shipyard Main Builder

on:
  push:
    branches: [main]
    paths: ["images/**"]

jobs:
  # JOB 1: Herausfinden, was sich geändert hat
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
      has_changes: ${{ steps.set-matrix.outputs.has_changes }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v47
        with:
          files: images/**
          dir_names: true
          dir_names_max_depth: 3
          matrix: true

      - name: Set matrix
        id: set-matrix
        # env:
        #   # Wir laden den Output in eine Environment-Variable.
        #   # Das verhindert das Escaping-Problem (Backslashes)!
        #   ALL_CHANGED_FILES: ${{ steps.changed-files.outputs.all_changed_files }}
        run: |
          CHANGED_DIRS='${{ steps.changed-files.outputs.all_changed_files }}'
          echo "changed dirs: $CHANGED_DIRS"

          if [ "$CHANGED_DIRS" == "[]" ] || [ -z "$CHANGED_DIRS" ]; then
            echo "Keine relevanten Änderungen gefunden."
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            CLEAN_MATRIX=$(echo "$CHANGED_DIRS" | jq -c 'map(split("/") | {name: .[1], variant: .[2], path: ("./" + join("/"))})')

            echo "Gefundene Änderungen in: $CLEAN_MATRIX"
            echo "matrix=$CLEAN_MATRIX" >> $GITHUB_OUTPUT
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi

  # JOB 2: Dynamisch bauen (ruft deinen Base-Workflow auf)
  build-images:
    needs: detect-changes
    if: needs.detect-changes.outputs.has_changes == 'true'
    strategy:
      fail-fast: false # Wenn ein Image fehlschlägt, bauen die anderen trotzdem weiter
      matrix:
        # HIER passiert die Magie: Die Liste der Images kommt aus Job 1
        image: ${{ fromJson(needs.detect-changes.outputs.matrix) }}

    # Ruft deinen bestehenden Base-Workflow auf
    uses: ./.github/workflows/_base.yaml
    permissions:
      packages: write
      attestations: write
      id-token: write
    with:
      image_name: ${{ matrix.image }}
      context_path: ./images/${{ matrix.image }}

  # changed-files:
  #   name: Get changed files
  #   runs-on: ubuntu-latest
  #   outputs:
  #     matrix: ${{ steps.changed-files.outputs.all_changed_files }}
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v6

  #     - name: Get changed files
  #       id: changed-files
  #       uses: ./
  #       with:
  #         files: images/**
  #         dir_names: true
  #         dir_names_max_depth: 3
  #         matrix: true

  #     - name: List all changed files
  #       run: echo '${{ steps.changed-files.outputs.all_changed_files }}'
